# Cloud Build uploads this directory as the build context. The Dockerfile runs
# npm ci itself, so shipping node_modules only made every build upload 73 MiB
# and 5,151 files to say the same thing.
.git
.gitignore
node_modules/
dist/
*.log
.env
.env.*

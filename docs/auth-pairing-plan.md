# iOS Safari authentication pairing

Implementation branch notes. The shipping design keeps the existing Google Identity Services flow on Android/desktop and adds a short-lived Safari pairing flow only for iOS Web Bluetooth browser contexts.

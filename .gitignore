# Firmware is maintained separately.
firmware/
esp/
*.ino
*.bin
*.elf
*.hex
*.zip

# Generated dependencies and local credentials.
node_modules/
.env
.env.*
!.env.example
*.log
.DS_Store

# Backend build output and Terraform state. State can carry resource metadata;
# keep it in a remote backend, never in the repo.
backend/dist/
infra/terraform/.terraform/
infra/terraform/*.tfstate
infra/terraform/*.tfstate.*
infra/terraform/*.tfvars

# Local browser test screenshots and fixtures.
artifacts/
